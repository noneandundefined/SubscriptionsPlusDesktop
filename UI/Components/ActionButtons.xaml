<UserControl x:Class="SubscriptionPlusDesktop.UI.Components.ActionButtons"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SubscriptionPlusDesktop.UI.Components"
             mc:Ignorable="d" 
             d:DesignHeight="150" d:DesignWidth="600">
    <Grid>
        <Border Cursor="Hand" 
                BorderBrush="#fff" 
                BorderThickness="1" 
                Padding="13" 
                CornerRadius="20" 
                HorizontalAlignment="Right" 
                VerticalAlignment="Center" 
                Margin="0,10,20,10"
                MouseLeftButtonUp="CreateSub_MouseLeftButtonUp">
            <Border.Effect>
                <DropShadowEffect Color="Black" Opacity="0.17" BlurRadius="10" ShadowDepth="1"/>
            </Border.Effect>

            <Image Source="pack://application:,,,/Public/ico/list-plus.png"
                   RenderOptions.BitmapScalingMode="HighQuality"
                   MaxWidth="20"
                   Cursor="Hand" />

            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Background" Value="#fff"/>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#f5f5f5"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
        </Border>
    </Grid>
</UserControl>
